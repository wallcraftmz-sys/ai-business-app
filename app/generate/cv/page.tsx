import GenerateClient from "../GenerateClient";

export default function Page() {
  return (
    <GenerateClient
      fixedType="cv"
      title="Резюме"
      subtitle="Создание CV и текстов для работы"
    />
  );
}
