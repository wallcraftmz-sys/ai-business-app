import GenerateClient from "../GenerateClient";

export default function EmailPage() {
  return (
    <GenerateClient
      fixedType="email"
      title="Email"
      subtitle="Создай письмо клиенту с помощью AI"
    />
  );
}