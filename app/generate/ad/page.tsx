import GenerateClient from "../GenerateClient";

export default function AdPage() {
  return (
    <GenerateClient
      fixedType="реклама"
      title="Реклама"
      subtitle="Создай рекламный текст с помощью AI"
    />
  );
}