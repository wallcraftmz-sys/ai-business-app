import GenerateClient from "../GenerateClient";

export default function Page() {
  return (
    <GenerateClient
      fixedType="study"
      title="Учёба"
      subtitle="Помощь с заданиями и текстами"
    />
  );
}
