import GenerateClient from "../GenerateClient";

export default function ReplyPage() {
  return (
    <GenerateClient
      fixedType="ответ"
      title="Ответ клиенту"
      subtitle="Подготовь ответ клиенту с помощью AI"
    />
  );
}